# Claude Instructions

## Git & Deployment
- **Never run `git commit` or `git push` unless the user explicitly asks.**
  Leave changes as modified files only. The user handles all committing and pushing themselves.
